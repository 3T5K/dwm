diff --git a/config.def.h b/config.def.h
index 4b07e6b..f1e61d5 100644
--- a/config.def.h
+++ b/config.def.h
@@ -2,6 +2,8 @@
 
 /* appearance */
 static const unsigned int borderpx  = 1;        /* border pixel of windows */
+static const unsigned int ogap      = 30;       /* gap on outer edges */
+static const unsigned int igap      = 10;       /* gap between windows */
 static const unsigned int snap      = 32;       /* snap pixel */
 static const int showbar            = 1;        /* 0 means no bar */
 static const int topbar             = 1;        /* 0 means bottom bar */
diff --git a/dwm.c b/dwm.c
index f35d8b5..f1cb8c9 100644
--- a/dwm.c
+++ b/dwm.c
@@ -1772,7 +1772,7 @@ tagmon(const Arg *arg)
 void
 tile(Monitor *m)
 {
-    unsigned int i, n, h, mw, my, ty, si, ma, sa;
+    unsigned int i, n, h, mw, my, ty, si, ma, sa, ns;
     Client *c, *xr[2];
     int em = m->pertag->exstacks[m->pertag->curtag].master
       , es = m->pertag->exstacks[m->pertag->curtag].stack;
@@ -1793,29 +1793,44 @@ tile(Monitor *m)
     if (!m->sel->isfloating)
         xr[si >= m->nmaster] = m->sel;
 
-    if (n > m->nmaster)
+    if (n > m->nmaster) {
         mw = m->nmaster ? m->ww * m->mfact : 0;
-    else
+        ns = m->nmaster > 0 ? 2 : 1;
+    } else {
         mw = m->ww;
+        ns = 1;
+    }
     for (i = ma = sa = my = ty = 0, c = nexttiled(m->clients); c; c = nexttiled(c->next), i++) {
         if (i < m->nmaster) {
             xraycfg( c
                    , em ? XrayGrpTileMaster : XrayGrpNone
                    , xr[0] == c ? (ma = 1, XrayAc) : XrayIn
                    );
-            h = em ? m->wh : (m->wh - my) / (MIN(n, m->nmaster) - i);
-            resize(c, m->wx, m->wy + my * !em, mw - (2*c->bw), h - (2*c->bw), 0);
-			if (my + HEIGHT(c) < m->wh)
-				my += HEIGHT(c);
+            h = em ? m->wh - 2 * ogap : (m->wh - 2 * ogap + igap - my) / (MIN(n, m->nmaster) - i);
+            resize( c
+                  , m->wx + ogap
+                  , m->wy + ogap + my * !em
+                  , mw - (ns ^ 3) * ogap - 2 * c->bw - (ns - 1) * (igap / 2)
+                  , h - 2 * c->bw - igap * !em
+                  , False
+                  );
+            if (my + HEIGHT(c) + igap < m->wh - 2 * ogap)
+                my += HEIGHT(c) + igap;
         } else {
             xraycfg( c
                    , es ? XrayGrpTileStack : XrayGrpNone
                    , xr[1] == c ? (sa = 1, XrayAc) : XrayIn
                    );
-            h = es ? m->wh : (m->wh - ty) / (n - i);
-            resize(c, m->wx + mw, m->wy + ty * !es, m->ww - mw - (2*c->bw), h - (2*c->bw), 0);
-            if (ty + HEIGHT(c) < m->wh)
-                ty += HEIGHT(c);
+            h = es ? m->wh - 2 * ogap : (m->wh - 2 * ogap + igap - ty) / (n - i);
+            resize( c
+                  , m->wx + (ns & 1) * ogap + mw + (ns - 1) * (igap / 2)
+                  , m->wy + ogap + ty * !es
+                  , m->ww - (ns ^ 3) * ogap - mw - 2 * c->bw - (ns - 1) * (igap / 2)
+                  , h - 2 * c->bw - igap * !es
+                  , False
+                  );
+            if (ty + HEIGHT(c) + igap < m->wh - 2 * ogap)
+                ty += HEIGHT(c) + igap;
         }
 
         xrayapply(c);
